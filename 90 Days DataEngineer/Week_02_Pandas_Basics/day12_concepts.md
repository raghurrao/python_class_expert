# Day 12: Data Aggregation & GroupBy

Aggregation allows summarizing columns. GroupBy allows segmenting records by category.

## Core Concepts
* `df.mean()`, `df.sum()`, `df.agg()`.
* `df.groupby('category_col')`: Group rows that share values.
* Aggregating grouped objects: `df.groupby('category').mean()`.
