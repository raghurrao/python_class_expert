# Day 16: Reshaping Data & Pivots

Today we learn wide-to-long and long-to-wide conversions.

## Core Concepts
* `df.pivot_table(values, index, columns, aggfunc)`.
* `pd.melt(df, id_vars, value_vars)`: Wide to long.
