# Day 17: SQLite in Python

Today we establish Python connections to SQLite relational databases.

## Core Concepts
* `sqlite3.connect('db_name')`.
* Executing SQL statements with cursor objects: `cursor.execute()`.
* Reading SQL tables into Pandas: `pd.read_sql_query()`.
